version: 0.0
os: linux

files:
  - source: docker-compose.yml
    destination: /home/ubuntu/app
    overwrite: true

hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 300
      runas: ubuntu
